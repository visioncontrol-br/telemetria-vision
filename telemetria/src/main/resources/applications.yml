spring:
  datasource:
    url: jdbc:postgresql://82.29.61.141:5432/mensageria
    username: postgres
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    host: 82.29.61.141
    port: 5672
    username: admin
    password: ${RABBITMQ_PASSWORD}
    virtual-host: /