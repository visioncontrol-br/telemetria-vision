spring:
  datasource:
    url: jdbc:postgresql://db.dmmdgekpwabkaitjwgkr.supabase.co:5432/postgres
    username: postgres
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    host: mensageria_rabbitmq
    port: 5672
    username: admin
    password: ${RABBITMQ_PASSWORD}
    virtual-host: /