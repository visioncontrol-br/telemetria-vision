spring:
  main:
    web-application-type: servlet

  datasource:
    # 1. Mudamos a porta de 5432 para 6543 (Porta oficial do Pooler IPv4 do Supabase)
    # 2. Mantemos o sslmode=require e o prepareThreshold=0
    url: "jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0"
    username: "postgres.aexerqizcetzpgbeazmi"

    # 3. Coloque a sua nova senha aqui, MANTENDO AS ASPAS DUPLAS por fora
    password: "@DevGestorADM159753"

    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: VisionControlHikariPool
      maximum-pool-size: 15
      minimum-idle: 2
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 20000

  # ... restante do arquivo continua igual (jpa, rabbitmq, etc)

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 50
        order_inserts: true

  rabbitmq:
    host: 82.29.61.141
    port: 5672
    username: "admin"
    password: "visioncontrolrabbit"
    virtual-host: /
    listener:
      simple:
        concurrency: 2
        max-concurrency: 5
        prefetch: 50

app:
  rabbitmq:
    queue: "fila-visioncontrol-prod"
    exchange: "visioncontrol-exchange"
    routing-key: "visioncontrol.routing.key"

server:
  port: 8080
  tomcat:
    basedir: ./.tomcat